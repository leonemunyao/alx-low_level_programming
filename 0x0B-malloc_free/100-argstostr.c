#include "main.h"
#include <stdlib.h>

/**
 * argstostr - concatenates all the arguments
 * @ac: the count of argument
 * @av: the vector of argument
 *
 * Return: Always 0
 */

char *argstostr(int ac, char **av)
{
char *point;
int i;
int j;
int k;

if (ac == 0)
{
return (NULL);
}
for (i = 0 ; )
}
