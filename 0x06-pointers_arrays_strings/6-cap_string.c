#include "main.h"

/**
 * cap_string - capitalizes all the words of a string
 * @str: the string
 * Return: the string
 */
char *cap_string(char *)
{
int a = 0;
for (a =  0 ; str[a] ; a++)
{
while (!(str[a] >= 'a' && str[a] <= 'z'))
}
}
